<h1>Create a new figure</h1>

<table>
  <th>Person Name</th>
  <tr>
    <% @figures.each do |figure| %>
      <td><a href="/figures/<%= figure.id %>"><%= figure.name %></a>
      <ul>
        <%figure.titles.each do |title|%>
        <li><%=title.name%></li>
        <%end%>
      </ul>
     </tr> </td>
  <% end %>
</table>

<a class="button" href="figure/new">Create New Figure</a>
<a class="button" href="/landmarks">View Landmarks</a>
<a class="button" href="/">Home</a>
